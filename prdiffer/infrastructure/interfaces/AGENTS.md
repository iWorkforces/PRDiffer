# AGENTS.md - Infrastructure/Interfaces

**Status: empty placeholder directory.**

Domain ports live under `prdiffer/domain/interfaces/` and `prdiffer/domain/services/`.
Infrastructure implements those ports; it does not define a parallel interfaces package today.
